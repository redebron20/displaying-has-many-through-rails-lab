<h2><%= @patient.name %></h2>

<% @patient.appointments.each do |appt| %>
  <%= link_to appt.doctor.name, doctor_path(appt.doctor) %> - <%= link_to appt.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(appt) %><br>
<% end %>